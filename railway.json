{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "./be/Dockerfile"
    },
  "deploy": {
    "preDeployCommand": ["npm run prisma:migration && npm run prisma:seeding"],
    "startCommand": "npm run start:prod"
  }
}